apiVersion: v1
kind: Service
metadata:
  name: {{ include "bgk-admin.fullname" . }}
  labels:
    {{- include "bgk-admin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bgk-admin.selectorLabels" . | nindent 4 }}
