apiVersion: v1
kind: Service
metadata:
  name: {{ include "bgk.fullname" . }}
  labels:
    {{- include "bgk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bgk.selectorLabels" . | nindent 4 }}
