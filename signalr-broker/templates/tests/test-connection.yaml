apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "signalr-broker.fullname" . }}-test-connection"
  labels:
    {{- include "signalr-broker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "signalr-broker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
