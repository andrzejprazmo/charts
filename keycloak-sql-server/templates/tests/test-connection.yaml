apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keycloak-sql-server.fullname" . }}-test-connection"
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "keycloak-sql-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keycloak-sql-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
