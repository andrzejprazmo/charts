apiVersion: v1
kind: Service
metadata:
  name: {{ include "kuberestapi.fullname" . }}
  labels:
    {{- include "kuberestapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kuberestapi.selectorLabels" . | nindent 4 }}
