apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kuberestapi.fullname" . }}-test-connection"
  labels:
    {{- include "kuberestapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kuberestapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
